# 引き継ぎ書 — ハレノヒ写真館 / AI Photo Studio

> このドキュメントは、**新しいClaude Codeセッション**(`akihiroyamauchi2023/ai-photo` リポジトリに直接書き込み可能なセッション)へ開発を引き継ぐためのものです。
> 読者は「開発を続けるClaude」と「オーナー(山内さん)」の両方を想定しています。

---

## 0. 最初にやること(新セッションのClaudeへ)

1. このリポジトリ **`akihiroyamauchi2023/ai-photo`** で作業します。**あなたはこのリポジトリに直接 push できます**(前セッションは `matou` にしか書けなかったため、わざわざローカル経由で同期していました。その手間はもう不要です)。
2. まず `README.md` と `DEPLOY.md` と本書を読んでください。
3. オーナーは**非エンジニア**です。§7「オーナー対応の注意」を必ず先に読んでください。
4. 変更後は必ず `npm run build` が通ることを確認してから push してください。

---

## 1. プロジェクト概要

**「手元の写真をアップロード → シーンとシチュエーションを選ぶ → AIがプロ品質の記念写真に仕上げる」** Webサービス。

- 対応シーン(9種): プロフィール写真 / 七五三 / 結婚式前撮り(和装) / 結婚式前撮り(洋装) / 遺影写真 / 成人式 / お宮参り・百日祝い / 家族写真 / マタニティ
- **ビジネスモデル: フリーミアム**。生成と透かし入りプレビューは無料、**高解像度ダウンロード時に課金**(Stripe)。
- ブランド名(既定): **ハレノヒ写真館 / HARENOHI PHOTO STUDIO**(`lib/branding.ts` で変更可)。

---

## 2. 技術スタック

- **Next.js 14 (App Router) + TypeScript**
- 画像処理: **sharp**
- AI画像生成: **Google Gemini** (`gemini-2.5-flash-image`, 通称 Nano Banana) の image-to-image
- 決済: **Stripe Checkout**
- データ保存: ファイルベース(`.data/`)。本番でスケールさせるなら S3/Cloud Storage + DB へ差し替え想定。
- 実行環境: Node.js 22。常駐Nodeサーバー前提(生成はバックグラウンド+ポーリング)。

---

## 3. 現在の状態(2026-07 時点)

### ✅ 動作確認済み
- サイト表示(ハレノヒ写真館ブランディング)
- フロー全体: シーン選択 → 写真アップロード → **シチュエーション複数選択** → 生成 → 透かしプレビュー → 購入 → ダウンロード
- **実際のAI画像生成**(オーナーの課金済みGeminiキーで成功確認済み)
- デモモード(APIキー無しでも全フロー確認可能)

### ⚠️ まだ本番設定でない
- **決済はデモモード**(Stripe未設定 = 「即成功」で動く)。実際に課金するには Stripe キー設定が必要(§6 TODO)。
- **未デプロイ**(オーナーのMacローカルでのみ稼働中)。公開は Xserver VPS 想定(`DEPLOY.md`)。

---

## 4. セットアップと実行

```bash
npm install
cp .env.example .env.local   # 値を設定(§5)
npm run dev                  # http://localhost:3000
npm run build                # 本番ビルド(push前に必ず確認)
```

APIキーが無くてもデモモードで全フローが動きます。

---

## 5. 環境変数(`.env.local`)

| 変数 | 用途 | 現状 |
|---|---|---|
| `GEMINI_API_KEY` | AI画像生成 | オーナーがローカルに**課金済みキー**を設定済み(コミットされない) |
| `GEMINI_IMAGE_MODEL` | モデル指定 | 省略可(既定 `gemini-2.5-flash-image`) |
| `GEN_CONCURRENCY` | 同時生成数 | 省略可(既定1=逐次)。課金キーなら2〜3に上げると高速 |
| `STRIPE_SECRET_KEY` | 決済 | **未設定**(デモ決済で動作中) |
| `STRIPE_WEBHOOK_SECRET` | Webhook検証 | 未設定 |
| `NEXT_PUBLIC_SITE_URL` | 決済リダイレクト先 | 既定 `http://localhost:3000` |
| `DATA_DIR` | 生成データ保存先 | 既定 `.data/` |
| `NEXT_PUBLIC_BRAND_*` | ブランド名/透かし | 省略可(既定=ハレノヒ写真館) |

> 🔐 **重要**: APIキー・トークンは `.env.local`(gitignore済み)にのみ置く。**チャットやコミットに絶対に含めない。**

---

## 6. アーキテクチャとファイル構成

```
app/
  page.tsx                  トップ(シーン一覧・料金・FAQ)
  studio/studio-client.tsx  撮影フロー(シーン→写真→シチュエーション選択)★UIの主役
  gallery/[jobId]/          生成結果(透かしプレビュー・写真選択・購入バー)
  success/                  購入完了・高解像度ダウンロード
  api/
    generate/               生成ジョブ作成(variationIds[]を受ける)
    job/[jobId]/            進捗ポーリング
    preview/.../            透かし入り無料プレビュー
    checkout/               Stripe Checkout セッション作成
    confirm/                決済検証(Webフック不達時のフォールバック兼用)
    webhook/stripe/         Stripe Webhook(checkout.session.completed)
    download/.../           購入済み写真の高解像度DL(トークン認証)
lib/
  scenes.ts     ★シーン定義(名称・説明・プロンプト・シチュエーション候補)。カスタマイズの中心
  branding.ts   ブランド名・透かし・ファイル名(1箇所で全体変更)
  generator.ts  生成ジョブエンジン(選択シチュエーションごとに1枚、並列度制御)
  gemini.ts     Gemini APIクライアント(429リトライ/バックオフ実装済み、デモ生成フォールバック)
  watermark.ts  プレビュー透かし
  pricing.ts    料金計算(パック料金の自動最適化、動的計画法)
  payments.ts   Stripe(未設定時はデモ決済)
  store.ts      ファイルベースのジョブストア(.data/)
```

### シーンとシチュエーションの仕組み(重要)
- `lib/scenes.ts` の `SCENES` 配列がすべての起点。ここを編集すればトップ・撮影フロー・生成に自動反映。
- 各シーンは `variations`(= ユーザーが選ぶ「シチュエーション候補」)を持つ。1つ選ぶ = 1枚生成。
- **現状の候補数/最大枚数**: 遺影のみ 10候補/最大5枚。**それ以外の8シーンは 16候補/最大8枚**。
- プロンプトは `basePrompt`(シーン共通) + `variations[].prompt`(シチュエーション別)を連結。顔の同一性維持を最優先する指示(`COMMON_QUALITY`)を含む。

---

## 7. オーナー対応の注意(必読)

- **日本語で対応する。**
- オーナーは**非エンジニア**。専門用語を避け、コマンドは**1行ずつ**・コピペしやすく提示する。
- **よくあるつまずき**(先回りして案内する):
  - 新しいターミナルは**ホームで開く**ため、毎回 `cd ai-photo` が必要(`package.json がない` エラーの原因)。
  - **Mac外付けキーボードで Control+C が効きにくい** → 「ターミナルを終了して開き直す」or 新タブで `pkill -f "next dev"` を案内。
  - `.env.local` を変更したら**サーバー再起動が必要**。
  - コード更新後はブラウザを**強制リロード(Cmd+Shift+R)**。
  - **APIキー/トークンをチャットに貼りがち** → 事前に「貼らないで」と伝え、もし貼られたら**そのキーの無効化・再発行**を促す。
- 変更を見せるときは、可能なら**スクリーンショット相当の説明**や、結果の確認方法まで示す。

---

## 8. 残タスク(TODO / 優先度順の目安)

1. **Stripe本設定** — 現在デモ決済。テストキー→本番キーの順で設定。Webhook(`/api/webhook/stripe`, `checkout.session.completed`)登録と `STRIPE_WEBHOOK_SECRET`。料金は `lib/pricing.ts`。
2. **生成品質・シーン調整** — 実際の生成結果を見てプロンプト(`lib/scenes.ts`)を改善。オーナー要望のシチュエーション追加/削除。
3. **インターネット公開** — Xserver **VPS**(レンタルサーバー/Staticは不可)。手順は `DEPLOY.md`。ドメイン→VPS、Nginx+SSL、PM2常駐。
4. **データ保存の本番化** — `.data/` ファイル保存を S3/Cloud Storage + DB へ(`lib/store.ts` が薄い抽象化層)。生成画像の保持期間・削除ポリシーも検討(`DEPLOY.md` に cron 例あり)。
5. **利用規約・プライバシー・特商法表記** — 課金サービスとして必要。遺影・子ども写真を扱うため配慮も。

---

## 9. 主要な設計判断(背景)

- **フリーミアム**: 生成無料・DL課金。プレビューは `sharp` で縮小+ブランド透かし、購入後にオリジナル配信。DLはジョブ毎トークン+購入済み検証で保護。
- **シチュエーション選択UI**: 「枚数だけ指定」→「候補から選ぶ(選んだ数=枚数)」に変更。候補は生成枚数の約2倍用意。
- **429対策**: Geminiの画像生成は無料枠がほぼ使えず課金必須。`gemini.ts` に指数バックオフのリトライ実装済み。既定は逐次生成(`GEN_CONCURRENCY=1`)。
- **デモモード**: キー未設定でも全フローを確認できるよう、生成・決済にフォールバックを用意。

---

## 10. リポジトリ経緯(参考)

- 当初 `akihiroyamauchi2023/matou` リポジトリの `claude/photo-transformation-system-g7vbev` ブランチで開発(前セッションの実行環境の制約でそこにしか書けなかった)。
- その後 **`akihiroyamauchi2023/ai-photo`(main ブランチ)へ移行**。**以後の正式な置き場所は `ai-photo`**。
- `matou` リポジトリの元コンテンツ(和装ブランドのランディングページ `matou-landing-page.html`)は本プロジェクトと無関係。`ai-photo` には含めていない。
- **新セッションは `ai-photo` に直接コミット/pushして進めてよい**(このスレッドを立てた目的)。
